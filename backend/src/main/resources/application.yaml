spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/firstfolio_db 
    username: postgres
    password: postgres123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect


server:
  port: 8080

jwt:
  secret: FirstFolioPortfolioPlatformSecretKey2026IshankaSonali
  expiration: 86400000